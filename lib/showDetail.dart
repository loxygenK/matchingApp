import 'package:flutter/material.dart';
class TestView extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: please write here show detail widget
    return Scaffold(
        appBar: AppBar(
          title: Text("詳細"),
        ),
        body: Text("テスト")
    );
  }
}